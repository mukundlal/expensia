import 'package:flutter/material.dart';

class DebitItemWidget extends StatelessWidget {
  const DebitItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
